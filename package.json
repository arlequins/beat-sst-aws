{
  "name": "beat-sst-aws",
  "private": true,
  "version": "0.6.3",
  "type": "module",
  "dependencies": {
    "aws-account-baseline-sst": "github:arlequins/template-sst-aws#v0.5.5"
  },
  "devDependencies": {
    "sst": "4.17.1",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "check": "tsc --project tsconfig.json",
    "install:sst": "sst install",
    "test": "node --test scripts/**/*.test.mjs",
    "diff": "sst diff",
    "deploy": "sst deploy",
    "verify": "pnpm check && pnpm test && pnpm install:sst"
  },
  "packageManager": "pnpm@10.33.0"
}
